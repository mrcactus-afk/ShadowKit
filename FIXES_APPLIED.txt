Critical fixes applied to ShadowKit:

1. Dynamic Paths:
   - Replaced hardcoded "C:\\ShadowKit" with \$PSScriptRoot in ShadowController.ps1.
   - Updated DNSFrenzy.ps1, ShadowLogger.ps1, and other components.

2. Graceful Shutdown:
   - Added stop.flag support in ShadowController.ps1.

3. PID File:
   - Added controller.pid for process management.

4. Log Rotation:
   - Added 7-day retention in ShadowLogger.ps1.

5. WMI Event Leaks:
   - Fixed in DNSFrenzy.ps1 by unregistering events on exit.

6. .gitignore:
   - Added to exclude Goose files, logs, and state files.